# Octop preview login. ADMIN_PASSWORD is required and must remain local.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# Reserved by the HFS v3.0 local credential model; Octop does not expose a
# separate ops token in this wrapper.
OPS_TOKEN=

# Local deployment control only; never synchronized into the Space runtime.
HF_TOKEN=
GH_TOKEN=
